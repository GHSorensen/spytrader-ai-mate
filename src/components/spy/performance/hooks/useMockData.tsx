
// Alias file to ensure backward compatibility
import { useMockPerformanceData } from './useMockPerformanceData';

// Export useMockPerformanceData as useMockData for backward compatibility
export const useMockData = useMockPerformanceData;
